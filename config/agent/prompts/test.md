---
description: Write tests for a file or function
---
Write tests for `$1`. If no path given, ask me what to test.

- Detect the project's existing test framework and follow its conventions
- Cover happy path, edge cases, and error cases
- Keep tests focused and readable — one assertion per concept
- Use descriptive test names that explain the expected behavior
- Mock external dependencies, not the thing being tested
